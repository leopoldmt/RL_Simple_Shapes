[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "simple_shapes_RL"
version = "0.0.1"
authors = [
    {name = "Léopold Maytié", email = "leopold.maytie@univ-tlse3.fr"},
]
requires-python = "==3.9"